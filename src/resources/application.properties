# Spring Boot application name
spring.application.name=SwiftTasks

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/swifttasks_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Anotaessasenha22))


# JPA (Java Persistence API) Configuration
spring.jpa.hibernate.ddl-auto=update  # Automatically create/update database schema
spring.jpa.show-sql=true              # Show SQL queries in the console
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect  # Set the dialect for MySQL 8.x

# Activate the 'dev' profile (optional based on your need)


# H2 Configuration (for optional local testing, comment out if you don't need it)
# Uncomment to use H2 instead of MySQL for local testing
# spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=password
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
